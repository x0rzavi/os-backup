# gcc + bfd + noLTO
CC="gcc"
CXX="g++"

COMMON_FLAGS="-march=native -O3 -pipe -fomit-frame-pointer"
LD="ld.bfd"
LDFLAGS="-Wl,-O1 -Wl,--as-needed"

AR="${CC}-ar"
NM="${CC}-nm"
RANLIB="${CC}-ranlib"
AS="${CHOST}-as"
STRIP="${CHOST}-strip"
